# วิธีอัปโหลดโปรเจคไป GitHub

## ขั้นตอนที่ 1: เตรียมโปรเจค

1. **ลบไฟล์ที่ไม่ต้องการ**
```bash
cd /Users/jamies/Library/CloudStorage/OneDrive-ส่วนบุคคล/Kiro/score-v2

# ลบไฟล์ test
rm -f test_save_grade.php

# ลบ database (จะสร้างใหม่ตอนติดตั้ง)
rm -f database/score.db
```

2. **ตรวจสอบไฟล์ที่จะอัปโหลด**
```bash
git status
```

---

## ขั้นตอนที่ 2: สร้าง GitHub Repository

1. ไปที่ https://github.com
2. คลิก **New repository**
3. ตั้งชื่อ: `score-v2` หรือชื่ออื่นที่ต้องการ
4. เลือก **Public** (ถ้าต้องการให้คนอื่นเห็น) หรือ **Private**
5. **อย่าเลือก** "Initialize this repository with a README"
6. คลิก **Create repository**

---

## ขั้นตอนที่ 3: อัปโหลดโปรเจค

### วิธีที่ 1: ใช้ Command Line (แนะนำ)

```bash
cd /Users/jamies/Library/CloudStorage/OneDrive-ส่วนบุคคล/Kiro/score-v2

# 1. Initialize git (ถ้ายังไม่ได้ทำ)
git init

# 2. Add ไฟล์ทั้งหมด
git add .

# 3. Commit
git commit -m "Initial commit: Student Grade & Attendance System"

# 4. เชื่อมต่อกับ GitHub (เปลี่ยน YOUR_USERNAME เป็นชื่อของคุณ)
git remote add origin https://github.com/YOUR_USERNAME/score-v2.git

# 5. Push ขึ้น GitHub
git branch -M main
git push -u origin main
```

### วิธีที่ 2: ใช้ GitHub Desktop

1. เปิด GitHub Desktop
2. คลิก **File** → **Add Local Repository**
3. เลือกโฟลเดอร์โปรเจค
4. คลิก **Publish repository**
5. เลือก **Public** หรือ **Private**
6. คลิก **Publish repository**

---

## ขั้นตอนที่ 4: แก้ไข README.md

1. เปิดไฟล์ `README.md`
2. แก้ไข URL ใน README:
   - เปลี่ยน `YOUR_USERNAME` เป็นชื่อ GitHub ของคุณ
   - เช่น: `https://github.com/jamies/score-v2`

3. Commit และ Push อีกครั้ง:
```bash
git add README.md
git commit -m "Update README with correct GitHub URL"
git push
```

---

## ขั้นตอนที่ 5: เพิ่ม Screenshots (Optional)

1. สร้างโฟลเดอร์ `screenshots` ในโปรเจค
2. ถ่ายภาพหน้าจอระบบ:
   - dashboard.png
   - grades.png
   - student-grades.png
3. ใส่ภาพในโฟลเดอร์ screenshots
4. Commit และ Push:
```bash
git add screenshots/
git commit -m "Add screenshots"
git push
```

---

## ขั้นตอนที่ 6: ทดสอบการติดตั้ง

ลองให้เพื่อนหรือคนอื่นติดตั้งโปรเจคของคุณ:

```bash
# Clone
git clone https://github.com/YOUR_USERNAME/score-v2.git
cd score-v2

# ติดตั้ง
php install.php
```

---

## 📝 Tips สำหรับ GitHub

### 1. เพิ่ม Topics
ไปที่ Repository → คลิก ⚙️ → Add topics:
- `php`
- `sqlite`
- `education`
- `student-management`
- `grade-system`

### 2. เพิ่ม Description
ไปที่ Repository → คลิก ⚙️ → About:
```
ระบบจัดการคะแนนและเวลาเรียนนักเรียน - Student Grade & Attendance Management System
```

### 3. เปิดใช้ GitHub Pages (Optional)
ถ้าต้องการทำ Demo Site:
1. Settings → Pages
2. Source: Deploy from a branch
3. Branch: main → /docs
4. Save

---

## 🔄 การอัปเดทโปรเจค

เมื่อมีการแก้ไขโค้ด:

```bash
# 1. Add ไฟล์ที่แก้ไข
git add .

# 2. Commit พร้อมข้อความ
git commit -m "Fix: แก้ไขปัญหา XYZ"

# 3. Push ขึ้น GitHub
git push
```

---

## ⚠️ สิ่งที่ต้องระวัง

❌ **อย่าอัปโหลด:**
- ไฟล์ database ที่มีข้อมูลจริง
- รหัสผ่าน หรือ API keys
- ไฟล์ส่วนตัว

✅ **ควรอัปโหลด:**
- Source code
- Schema database (ไม่ใช่ไฟล์ .db)
- Documentation
- LICENSE

---

## 🎉 เสร็จแล้ว!

โปรเจคของคุณพร้อมแชร์บน GitHub แล้ว!

URL: `https://github.com/YOUR_USERNAME/score-v2`
